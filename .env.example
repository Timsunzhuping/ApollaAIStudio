# Apolla AI — environment variables (copy to .env; never commit real keys)
# Model provider keys (names referenced by routes.json keyPool)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Tool providers (Sprint 01 T6 — falls back to deterministic stub when unset)
TAVILY_API_KEY=
