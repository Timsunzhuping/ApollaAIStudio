{
  "name": "apolla",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "vitest run",
    "dev": "echo \"[apolla] dev server lands in Sprint 01 T12/T13\" && exit 0",
    "eval": "echo \"[apolla] eval harness lands in Sprint 01 T14\" && exit 0",
    "contract-test": "echo \"[apolla] provider contract tests land in Sprint 01 T4\" && exit 0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "eslint": "^9.15.0",
    "prettier": "^3.4.2",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.16.0",
    "vitest": "^2.1.8"
  }
}
