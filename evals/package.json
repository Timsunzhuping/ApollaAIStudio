{
  "name": "@apolla/evals",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LLM product evals — deterministic research golden + citation/cost regression gates.",
  "scripts": {
    "eval": "tsx runner.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "eval:live": "tsx live/runner.ts"
  },
  "dependencies": {
    "@apolla/bff": "workspace:*",
    "@apolla/contracts": "workspace:*",
    "@apolla/harness-core": "workspace:*",
    "@apolla/mcp-stdio": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
